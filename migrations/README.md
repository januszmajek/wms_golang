# Migrations

Future database migrations will live here. PostgreSQL is planned, and goose is selected as the migration tool.

Do not add migration files until a persistence-focused slice defines the required schema change.

